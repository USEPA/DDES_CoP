install npm and angular: https://angular.io/guide/quickstart

Generate a new angular app by running the following command:
ng new ddes-rest

cd into that directory and run ng-serve

I then added the service, model and display logic which can be found in the project.
